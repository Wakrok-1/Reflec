// Character class badge — gradient generated deterministically from the
// class name via classNameToGradient (design spec v1.0, section 8).
// Stub for Sprint 2.
interface ClassBadgeProps {
  name: string
}

export function ClassBadge(_props: ClassBadgeProps) {
  return null
}
